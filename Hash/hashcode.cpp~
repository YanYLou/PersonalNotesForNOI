nclude<bits/stdc++.h>
using namespace std;
#define int long long
const int N = 1e5 + 5;
const int seed = 131;

signed main() {
	    ios::sync_with_stdio(0); cin.tie(0);
	        char s[N];
		    cin >> (s + 1);
		        int n = strlen(s + 1);
			    int hash = 0;
			        for (int i = 1; i <= n; ++i) {
					        hash = hash * seed + s[i];
						    }
				    cout << hex << hash << '\n';
				        return 0;
}
